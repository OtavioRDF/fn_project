export enum UserAccountTypeEnum {
  Epic = 'epic',
  Psn = 'psn',
  Xbl = 'xbl',
}

export enum UserTimeWindowEnum {
  Season = 'season',
  lifetime = 'lifetime',
}

export enum UserImageEnum {
  All = 'all',
  keyboardMouse = 'keyboardMouse',
  Gamepad = 'gamepad',
  Touch = 'touch',
  None = 'none',
}
